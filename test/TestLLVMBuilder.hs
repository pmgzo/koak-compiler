module TestLLVMBuilder where
import Test.HUnit
import LLVM_Builder

-- resTest9ParserList = (Just ((Value (Quote "(coco)") EndList), ""))
-- test9ParserList = TestCase $ assertEqual "   '(coco)" resTest9ParserList (parseList "   '(coco)")



llvmBuilderTests = TestList []
