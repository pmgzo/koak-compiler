def factorial(x : int):int x * factorial(x - 1);
factorial(4);

def factorial2(x : int):int
    y = x:
    x = x - 1:
    while x > 0 do y = y * x : x = x - 1;
    y;
factorial2(4)

def factorial3(x : int):int
    y = x:
    for x = x - 1, x > 0, x = x - 1 in y = y * x;
    y;
factorial3(4)