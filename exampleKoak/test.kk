def factorial(a): Int
    if a == 1
        1
    else a > 1
        a * call(a - 1)

def sum(a, b): Int
    a + b

def fact2(a): Int
    res = 1
    while a != 0 do
        res = res * a
        a = a - 1
    res

def fct1(a, b): Int
    