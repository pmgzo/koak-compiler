def factorial(a : int): int
    if a == 1 then
        1
    else a > 1
        a * call(a - 1);

def sum(a : int, b : int): int
    a + b;

def fact2(a : int): int
    res = 1:
    while a != 0 do
        res = res * a:
        a = a - 1,
    res;

# def fct1(a : int, b : int): Int;
